# 複製此檔案為 .env 並填入實際值
# cp .env.example .env

# PostgreSQL
DB_USER=ont_user
DB_PASSWORD=changeme_strong_password

# JWT Secret (產生方式: openssl rand -hex 32)
SECRET_KEY=changeme_generate_with_openssl_rand_hex_32

# CORS (生產環境請改為實際網域)
ALLOWED_ORIGINS=http://localhost
